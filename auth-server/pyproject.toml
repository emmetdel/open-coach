[project]
name = "opencoach-auth"
version = "0.1.0"
description = "Garmin OAuth authentication server with MFA support for OpenCoach"
requires-python = ">=3.11"
dependencies = [
    "garth>=0.4.0",
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
    "python-dotenv>=1.0.0",
]
